function switchOfStuff(val) {
  var answer = "";
  // Only change code below this line

  switch (val) {
    case "a":
      answer = "apple";
      break;
      
  // Only change code above this line
  return answer;
}

switchOfStuff(1);